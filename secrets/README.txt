Esta carpeta se utiliza únicamente en la alternativa Docker.

La clave real debe llamarse google-drive-reader.json y montarse como archivo de
sólo lectura. No incluirla en el ZIP, GitHub, correo ni chat.

En una instalación directa, guardar la clave fuera del código en:
/etc/control-puntos-docentes/google-drive-reader.json
